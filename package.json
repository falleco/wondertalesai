{
  "name": "@mailestro/root",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn run --filter @mailestro/* dev",
    "build": "bun run --filter @mailestro/* build",
    "------------------------- LINTING -------------------------": "",
    "check": "biome check .",
    "check:fix": "biome check --write ."
  },
  "packageManager": "yarn@4.9.2",
  "engines": {
    "node": ">=22.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "typescript": "~5.9.3"
  },
  "trustedDependencies": [
    "@nestjs/core",
    "@socialpunk/backend",
    "@swc/core",
    "@tailwindcss/oxide",
    "protobufjs"
  ],
  "resolutions": {
    "typeorm": "0.3.28"
  }
}
